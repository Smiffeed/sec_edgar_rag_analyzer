[project]
name = "sec-edgar-rag-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apache-airflow>=3.3.1",
    "bandit>=1.9.4",
    "chromadb>=1.5.9",
    "dotenv>=0.9.9",
    "pytest>=9.1.1",
    "ruff>=0.16.5",
    "sec-edgar-downloader>=5.1.0",
    "unstructured>=0.24.1",
]

[tool.pytest.ini_options]
pythonpath = [
  "."
]

[tool.ruff.lint.isort]
known-first-party = ["src", "airflow"]
